node_modules/
.next/
.next_bak_*/
.netlify/
.env
.env.*
!.env.example
.DS_Store
coverage/
tmp.json
*.tsbuildinfo
.temp 
node-*
orchestration/*
# Root-only: do not use bare `thumbnail-*` — it ignores lib/thumbnail-*.ts and breaks builds.
/thumbnail-*
certificates

# Cursor debug session logs (do not commit)
.cursor/debug*.log
.vercel

# Claude Code worktrees (isolated feature branches, not tracked)
.claude/worktrees/
.worktrees/

# Fallow
.fallow

# Stray npm artifact (repo uses pnpm)
package-lock.json

# Local DB backups taken before migrations
backups/

tmp/bc2-titles.json
tmp/bc2-title-audit.csv
tmp/bc2-title-audit.json
tmp/bc2-import-orphans.csv
tmp/bc2-import-summary.json

# Audit + ad-hoc tooling output
tmp/

# OMC runtime state (rewritten live by tooling)
.omc/state/hud-stdin-cache.json
.omc/state/subagent-tracking.json
.omc/state/agent-replay-*.jsonl
.omc/state/*
